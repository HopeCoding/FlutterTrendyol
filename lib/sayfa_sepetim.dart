import 'package:flutter/material.dart';

class SayfaSepetim extends StatelessWidget {
  const SayfaSepetim({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
