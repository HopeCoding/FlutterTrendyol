import 'package:flutter/material.dart';

class SayfaFavoriler extends StatelessWidget {
  const SayfaFavoriler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
